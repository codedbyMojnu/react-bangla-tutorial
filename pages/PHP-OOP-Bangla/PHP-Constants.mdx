# PHP OOP - ক্লাস কনস্ট্যান্ট

[আগের পৃষ্ঠা](php_oop_inheritance.asp) | [পরবর্তী পৃষ্ঠা](php_oop_classes_abstract.asp)

---

## PHP - ক্লাস কনস্ট্যান্ট কী?

ক্লাসের ভেতরে যদি কোনো স্থায়ী মান (যা কখনো পরিবর্তন হবে না) রাখতে চান, তাহলে ক্লাস কনস্ট্যান্ট ব্যবহার করা হয়।

ক্লাস কনস্ট্যান্ট ডিক্লেয়ার করতে `const` কীওয়ার্ড ব্যবহার করতে হয়।  
একবার ডিক্লেয়ার করলে, সেই কনস্ট্যান্টের মান আর পরিবর্তন করা যায় না।

কনস্ট্যান্টগুলো কেস-সেন্সিটিভ, তবে সাধারণত সব অক্ষর বড় (UPPERCASE) দিয়ে লেখা হয়।

---

### ক্লাস কনস্ট্যান্ট অ্যাক্সেস করার নিয়ম

ক্লাসের বাইরে থেকে কনস্ট্যান্ট অ্যাক্সেস করতে হয়:  
`ক্লাসের_নাম::কনস্ট্যান্ট_নাম`

```php
<?php
class Goodbye {
  const LEAVING_MESSAGE = "Thank you for visiting W3Schools.com!";
}

echo Goodbye::LEAVING_MESSAGE;
?>
```

---

ক্লাসের ভেতর থেকে কনস্ট্যান্ট অ্যাক্সেস করতে হয়:  
`self::কনস্ট্যান্ট_নাম`

```php
<?php
class Goodbye {
  const LEAVING_MESSAGE = "Thank you for visiting W3Schools.com!";
  public function byebye() {
    echo self::LEAVING_MESSAGE;
  }
}

$goodbye = new Goodbye();
$goodbye->byebye();
?>
```

---
