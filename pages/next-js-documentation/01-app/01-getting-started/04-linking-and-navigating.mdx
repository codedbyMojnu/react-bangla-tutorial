# Linking আর Navigating – Next.js এ কীভাবে কাজ করে?

ভাবো তুমি একটা **Daraz type e-commerce ওয়েবসাইট** বানাচ্ছো।
ইউজার এক পেজ থেকে আরেক পেজে যাবে – যেমন **Home থেকে Product Details**, বা **Cart থেকে Checkout**।

এখন প্রশ্ন হচ্ছে, ইউজার যখন লিঙ্কে ক্লিক করবে তখন আসলে কী হবে?
পুরো পেজ কি আবার reload হবে? নাকি শুধু যেটুকু দরকার সেটুকুই বদলাবে?

এই blog-এ আমি তোমাকে বড় ভাইয়ের মতো করে বুঝাচ্ছি – Next.js এর **Linking আর Navigating** কীভাবে কাজ করে, আর কীভাবে এটাকে **দ্রুত, smooth** করা যায়।

---

<AudioPlayer
  src="/audios/linking-and-navigating-nextjs.wav"
  title="লিঙ্কিং এবং নেভিগেটিং ইন নেক্সট জেএস"
/>

## ১. Server Rendering – আসল গল্প শুরু এখান থেকে

Next.js by default server এ render করে।
মানে, তুমি যখন `/blog` এ যাবে তখন server থেকে response আসবে, তারপর পেজ লোড হবে।

এটার দুই রকম স্টাইল আছে –

- **Static Rendering (Prerendering)** 👉 ধরো তোমার সাইটে একটা **About Us** পেজ আছে। এটা তো বারবার change হয় না। Next.js এটা আগে থেকেই বানিয়ে রাখবে build time এ।
- **Dynamic Rendering** 👉 ধরো তোমার সাইটে **Order Details** পেজ আছে। User ID বা Order ID এর উপর ভিত্তি করে এই data আলাদা হবে। তাই যখনই ইউজার যাবে তখনই server fresh বানিয়ে দেবে।

---

## ২. Prefetching – আগেভাগেই খাবার রান্না

ধরো তুমি একটা guest আসবে জানো। তুমি guest আসার আগে রান্না করে রাখলে – guest আসামাত্র তাকে serve করতে পারবে।
এটাই prefetching!

Next.js এ `<Link>` component automatically prefetch করে।

```jsx
import Link from "next/link";

export default function Layout() {
  return (
    <nav>
      {/* এইটা prefetch হবে */}
      <Link href="/blog">Blog</Link>

      {/* এইটা হবে normal link */}
      <a href="/contact">Contact</a>
    </nav>
  );
}
```

👉 এখানে Blog লিঙ্ক viewport এ এলেই Next.js background এ ডাটা নিয়ে আসবে।
তাহলে ইউজার ক্লিক করার সাথে সাথে Blog পেজ ফ্লাই করে খুলে যাবে।
কিন্তু Contact এর ক্ষেত্রে prefetch হবে না, তাই একটু সময় লাগতে পারে।

---

## ৩. Streaming – ধীরে ধীরে খাবার সার্ভ করা

কখনও রান্না বড় হয়, ধরো **বিরিয়ানি**। সবকিছু একসাথে পরিবেশন করতে গেলে দেরি হবে।
তাই তুমি আগে সালাদ দিয়ে দিলে, তারপর বিরিয়ানি এলো, শেষে মিষ্টি।
👉 ইউজার (guest) খালি টেবিল দেখে বিরক্ত হলো না।

এটাই streaming।

Next.js এ তুমি শুধু একটা `loading.tsx` বানিয়ে দিলেই, Next.js প্রথমে loading দেখাবে, তারপর আসল content দেখাবে।

```tsx
// app/dashboard/loading.tsx
export default function Loading() {
  return <p>লোড হচ্ছে...</p>;
}
```

👉 এতে user বুঝতে পারবে – “হ্যাঁ, সাইট কাজ করছে, data আসছে।”

---

## ৪. Client-side Transition – reload ছাড়া মজা

পুরোনো ওয়েবসাইটে যখনই পেজ change করতে, পুরো পেজ reload হতো। Scrollbar reset হয়ে যেত, সবকিছু আবার লোড হতো।

Next.js `<Link>` দিয়ে এটা করে না। শুধু content change হয়, কিন্তু navbar, footer, scroll state ঠিক থাকে।

এটা ঠিক যেমন তুমি **Facebook এ Newsfeed scroll করছো**। নতুন পোস্ট খুললেও পুরো সাইট reload হয় না।

---

## ৫. কখন Navigation Slow মনে হয়?

সবকিছু সুন্দর হলেও কিছু সময় problem হতে পারে –

1. **Dynamic route এ loading.tsx নাই** 👉 `/blog/[slug]` এ গেলে ইউজার খালি screen দেখে বিরক্ত হবে। তাই অবশ্যই loading UI দাও।
2. **Slow network** 👉 গ্রামে 3G তে বসা ইউজার। Prefetch শেষ হওয়ার আগেই click করলে delay লাগতে পারে। Solution → ছোট একটা spinner বা progress bar দেখাও।
3. **অনেক লিঙ্ক একসাথে prefetch করলে** 👉 ধরো university noticeboard এ ১০০০ টা লিঙ্ক আছে। সব prefetch করলে server চাপ খাবে। Solution → শুধু hover করলে prefetch করো।

---

## ৬. Real Bangladeshi Example – History API

### (ক) Product Sorting (pushState)

Daraz এ গিয়েছো → Product list দেখছো। এখন “Low to High” দিয়ে sort করছো।
URL change হচ্ছে, কিন্তু পেজ reload হচ্ছে না।

```jsx
"use client";
import { useSearchParams } from "next/navigation";

export default function SortProducts() {
  const searchParams = useSearchParams();

  function updateSorting(order) {
    const params = new URLSearchParams(searchParams.toString());
    params.set("sort", order);
    window.history.pushState(null, "", `?${params.toString()}`);
  }

  return (
    <>
      <button onClick={() => updateSorting("asc")}>Low to High</button>
      <button onClick={() => updateSorting("desc")}>High to Low</button>
    </>
  );
}
```

👉 এখানে back button চাপলে আগের sorting এ ফিরতে পারবে।

---

### (খ) Language Switch (replaceState)

ধরো সরকারি ওয়েবসাইটে English ↔ Bangla switch করছো।
এখানে back চাপলে আগের ভাষায় ফিরে যাওয়া যাবে না।

```jsx
"use client";
import { usePathname } from "next/navigation";

export function LocaleSwitcher() {
  const pathname = usePathname();

  function switchLocale(locale) {
    const newPath = `/${locale}${pathname}`;
    window.history.replaceState(null, "", newPath);
  }

  return (
    <>
      <button onClick={() => switchLocale("en")}>English</button>
      <button onClick={() => switchLocale("bn")}>বাংলা</button>
    </>
  );
}
```

---

## শেষ কথা

Next.js এ Linking আর Navigating আসলে অনেক simple –

- Prefetching আগেভাগে ডাটা নিয়ে আসে
- Streaming loading UI দেখায়
- Client-side transition reload ছাড়াই smooth experience দেয়

বাংলাদেশি real life example এ ধরো –
তুমি **Daraz এ প্রোডাক্ট ব্রাউজ করছো**, **Bkash app এ পেজ change করছো**, বা **Facebook এ পোস্ট scroll করছো**।
এই সব জায়গায় user smooth experience পায় কারণ তারা এই optimizations use করছে।

Next.js আমাদের একই জিনিস ready-made দিয়েই দিয়েছে। শুধু সঠিকভাবে use করতে হবে। 🚀

---
