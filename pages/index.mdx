---
title: React JS Bangla Tutorial
description: React JS Bangla Tutorial / Course - Step by Step Guide
author: Md. Mojnu Miah
---

## 👋 স্বাগতম React Bangla Tutorial-এ!

```jsx
function Welcome() {
  return <h1>React.js Bangla Tutorial এ আপনাকে স্বাগতম!</h1>;
}
```

ভাই, যদি তুমি একদম নতুন হয়ে থাকো, কিংবা আগেও চেষ্টা করেছিলে কিন্তু থেমে গেছো—তাহলে এই React Bangla Tutorial তোমার জন্যই বানানো হয়েছে। আমরা React-এর অফিসিয়াল ডকুমেন্টেশনকে ফলো করে, একদম সহজ বাংলায়, গল্পের মতো করে শিখিয়ে দেবো। যেন তুমিও হাতে-কলমে কোড করতে করতে একজন আত্মবিশ্বাসী ফ্রন্টএন্ড React ডেভেলপার হয়ে উঠতে পারো।

> 🎯 আমাদের টার্গেট শুধু শেখানো না — বরং তুমি যেন নিজে থেকে প্র্যাকটিস করে শিখতে পারো, সেই শক্তি তোমার মধ্যে তৈরি করা। যে নিজে নিজে চিন্তা করে অ্যাপ বানাতে পারে।

---

### ❗ একটা জিনিস আগে থেকেই জানিয়ে রাখি...

> শুধু টিউটোরিয়াল পড়লে বা ভিডিও দেখলে কখনোই ভালো ডেভেলপার হওয়া যায় না। আসল খেলাটা শুরু হয় যখন তুমি **নিজে হাতে কোড করো, ভুল করো, এবং সেই ভুল থেকে শেখো**। ব্যাপারটা অনেকটা সাইকেল চালানো শেখার মতো। তুমি কি শুধু ইউটিউবে ভিডিও দেখেই সাইকেল চালানো শিখে ফেলতে পারবে? পারবে না। তোমাকে সাইকেলে চড়তে হবে, প্যাডেল করতে হবে, পড়তে হবে, হয়তো দুই-একবার হাঁটুতেও ছিলে যাবে।

**প্র্যাকটিসই আসল শিক্ষা।** ভুল করো, আবার শিখো, আর বারবার চেষ্টা করে যাও। এই টিউটোরিয়ালে আমরা শুধু থিওরি না, রিয়েল কোড উদাহরণসহ দেখাবো—তোমার কাজ শুধু একটা: **হাল না ছাড়া**। আটকে গেলে ভয় পেয়ো না, কারণ আমরা সবাই একসাথেই শিখছি।

---

### 🤔 এই টিউটোরিয়াল কাদের জন্য?

- যারা একদম নতুন এবং React শিখতে চায় এবং React-এর 'R'-ও জানে না।
- যারা আগে React শুরু করেছিল, মাঝপথে হাল ছেড়ে দিয়েছিলে।
- যারা অফিসিয়াল ডকুমেন্টেশন পড়তে গিয়ে ইংরেজিতে হিমশিম খায়

---

### 🧭 এই পাঠে তুমি কী শিখবে?

- React কী এবং কেন দরকার? কেন এটা এত জনপ্রিয়?
- কিভাবে ছোট ছোট Component বানিয়ে গোছানো UI বানানো যায়
- JSX কী, আর কেন এটা HTML থেকেও শক্তিশালী?
- কিভাবে State, Props, Event, এবং Hooks দিয়ে Interactive UI বানাতে হয়
- রিয়েল লাইফ ছোট প্রজেক্ট তৈরি
- React Native ও Next.js এর ঝলক
- আর Vanilla JS vs React নিয়ে পরিষ্কার ধারণা

---

## React.js-এর ইতিহাস

২০১১ সালে ফেসবুকের **Jordan Walke** React তৈরি করেন। তিনি PHP-এর Component-Based Framework "XHP" থেকে অনুপ্রাণিত হয়ে এটি ডেভেলপ করেন। React প্রথম ফেসবুকের নিউজফিডে এবং পরে ২০১২ সালে ইনস্টাগ্রামে ব্যবহার করা হয়।

২০১৩ সালে ফেসবুক React.js-কে ওপেন সোর্স ঘোষণা করে। এরপর থেকে React দ্রুত জনপ্রিয়তা পায় এবং বিভিন্ন বড় কোম্পানি ও ডেভেলপারদের মধ্যে ছড়িয়ে পড়ে।

## ✨ চল শুরু করি আমাদের React শেখার যাত্রা

ভাই,মনে রেখো, **তুমি একা নও**। এই যাত্রাটাও একা একা না। আমরা সবাই একসাথে শিখবো। তুমি, আমি, আর হাজারো নতুন ডেভেলপার মিলে সামনে এগোবো।

---

## রিয়্যাক্ট কী?

React হলো JavaScript-এর একটা লাইব্রেরি যেটা দিয়ে খুব সহজে UI (User Interface) বানানো যায়। আর UI মানে যা ব্যবহারকারী ব্রাউজারে দেখে এবং ব্যবহার করে।

> যেমন ফেসবুকের নিউজফিড, ইউটিউবের ভিডিও লিস্ট বা ইনপুট ফর্ম — এই সব কিছুই UI।

React একসাথে কাজ করে ওয়েব, মোবাইল—সব প্ল্যাটফর্মেই।

---

## React-এর সবচেয়ে গুরুত্বপূর্ণ আইডিয়াগুলো:

### 🔹 ছোট ছোট টুকরো জুড়ে দিয়ে ইন্টারফেস বানানো

React-এ আমরা পুরো ওয়েবসাইটকে একবারে বানাই না। React বলে, **"পুরো জিনিস একসাথে বানাতে যাও না ভাই, টুকরা টুকরা করে বানাও!"** প্রতিটা ছোট টুকরাকে বলে **Component**।

> এইভাবে কম্পোনেন্ট-ভিত্তিক কাজ করার সবচেয়ে বড় সুবিধা হলো, কোড খুব গোছানো থাকে এবং টিমের সাথে কাজ করা সহজ হয়। প্রত্যেকে নিজের কম্পোনেন্ট নিয়ে কাজ করতে পারে।

ধরো, তুমি YouTube-এর মতো একটি পেজ বানাচ্ছো। সেখানে থাকতে পারে:

- `<Thumbnail />` — ছবিটা
- `<LikeButton />` — লাইক বাটন
- `<Video />` — পুরো ভিডিও ব্লক

এই Component গুলোকে একসাথে জোড়া দিয়ে বানিয়ে ফেলা যায় একটা আস্ত অ্যাপ।

```jsx
// Video.js
function Video({ video }) {
  return (
    <div>
      <VideoThumbnail image={video.thumbnail} />
      <a href={video.url}>
        <VideoTitle title={video.title} />
        <p>{video.description}</p>
      </a>
      <LikeButton />
    </div>
  );
}
```

---

### 🔹 JSX — যেখানে কোড আর ডিজাইন একসাথে থাকে

React কম্পোনেন্ট আদতে একটি জাভাস্ক্রিপ্ট ফাংশন। React-এ আমরা যে কোড লিখি সেটা JSX। দেখতে যেমন HTML, কিন্তু এর ভিতর JS-এর সমস্ত পাওয়ার আছে।

```jsx
return <h1>{heading}</h1>;
```

এখানে `{heading}` হচ্ছে JavaScript expression, আর `<h1>` হচ্ছে JSX tag।

React এটাকে জাভাস্ক্রিপ্টে রূপান্তর করে, যা ব্রাউজার বুঝতে পারে। পর্দার আড়ালে এটা JSX, মূলত ব্রাউজারে যাওয়ার আগে এইরকম কোডে রূপ নেয়:

```js
React.createElement("h1", null, "Welcome!");
```

JSX থাকায় কোড ক্লিন, বোঝা সহজ এবং মেইনটেইন করা সুবিধাজনক।

<hr />
ব্যাপারটা হলো, JSX তোমাকে HTML-এর ভেতরেই জাভাস্ক্রিপ্টের শক্তি ব্যবহার করার সুযোগ
দেয়। এর ফলে ডিজাইন (মার্কআপ) আর লজিক এক জায়গায় থাকে, যা কোড বোঝা এবং ম্যানেজ করা
অনেক সহজ করে দেয়।

**উদাহরণ:**

```jsx
// VideoList.js
function VideoList({ videos, emptyHeading }) {
  const count = videos.length;
  let heading = emptyHeading;

  if (count > 0) {
    const noun = count > 1 ? "videos" : "video";
    heading = count + " " + noun;
  }

  return (
    <section>
      <h1>{heading}</h1>
      {/* এখানে .map() ব্যবহার করে প্রতিটা ভিডিওর জন্য একটি কম্পোনেন্ট দেখানো হচ্ছে */}
      {videos.map((video) => (
        <Video key={video.id} video={video} />
      ))}
    </section>
  );
}
```

> **প্রশ্ন:** `<Welcome/>` কম্পোনেন্টের `<h1>` ট্যাগটা কি আসল HTML?

---

### 🔹 যেখানেই দরকার, সেখানেই ইন্টারঅ্যাকশন

React দিয়ে তুমি চাইলে পুরো ওয়েবসাইট নতুন করে না বানিয়ে, তোমার পুরনো ওয়েবসাইটের যেকোনো একটি অংশে ডাইনামিক ফিচার যোগ করতে পারো। যেমন, একটা লাইভ সার্চ বার।

নিচের উদাহরণে, তুমি যখন সার্চ বক্সে কিছু টাইপ করবে, React সাথে সাথে ভিডিও লিস্ট ফিল্টার করে দেখাবে।

> **নোট:** নিচের কোডটি এখন পুরোপুরি বোঝার দরকার নেই। শুধু একবার চোখ বুলিয়ে রাখো যে কীভাবে একটি ইন্টারঅ্যাকটিভ লিস্ট বানানো যায়।

```jsx
// SearchableVideoList.js

// ... (এখানে ভিডিওর ডেটা এবং অন্যান্য কম্পোনেন্ট থাকবে) ...

function SearchableVideoList({ videos }) {
  const [searchText, setSearchText] = useState("");
  const foundVideos = filterVideos(videos, searchText); // ফিল্টার করা ভিডিও

  return (
    <>
      <SearchInput value={searchText} onChange={setSearchText} />
      <VideoList
        videos={foundVideos}
        emptyHeading={`"${searchText}" দিয়ে কিছু পাওয়া যায়নি`}
      />
    </>
  );
}
```

ব্যাস, UI এখন তোমার ইনপুটের উপর রিয়েল টাইমে রিঅ্যাক্ট করবে।

---

### 🔹 চাইলে Next.js দিয়ে ফুলস্ট্যাক অ্যাপ বানাও

React শুধু UI বানায়। কিন্তু বড় অ্যাপ বানাতে দরকার হয় রাউটিং, সার্ভার থেকে ডেটা ফেচিং ইত্যাদি।

এই কাজগুলো সহজ করতে আমরা Next.js ব্যবহার করতে পারি। এতে SSR (Server Side Rendering), API Routes, Image Optimization সবই তৈরি থাকে।

---

### 🔹 শুধু ওয়েব না, মোবাইল অ্যাপও বানাও!

React Native দিয়ে তুমি Android ও iOS অ্যাপ বানাতে পারো, একদম নেটিভ ফিল সহ। Expo ব্যবহার করলে আরো সহজ হয়।

মানে, এক স্কিল — দুই প্ল্যাটফর্ম! 🚀

---

### 🔹 তুমি একা না — আমরা সবাই একসাথে শিখি

প্রতি মাসে ২০ লাখ ডেভেলপার React ডকুমেন্টেশন পড়ে শেখে। কেউ শুরু করে, কেউ শিখিয়ে দেয়।
তুমি চাইলে GitHub, StackOverflow, Discord, Reddit, YouTube, এমনকি Facebook Group-এ কমিউনিটির সঙ্গে যুক্ত হয়ে শিখতে পারো।

---

### 🔹 পুরোনো প্রজেক্টেও React যুক্ত করা সম্ভব

React শেখার জন্য তোমাকে নতুন অ্যাপ বানাতেই হবে এমন না।
চাইলেই তোমার পুরোনো ওয়েবসাইটে React কম্পোনেন্ট যুক্ত করতে পারো:

```html
<nav id="navigation"></nav>
```

```js
import { createRoot } from "react-dom/client";

function NavigationBar() {
  return <h1>Hello from React!</h1>;
}

const root = createRoot(document.getElementById("navigation"));
root.render(<NavigationBar />);
```

---

### 🔹 React-এর মূল কিছু ফিচার এক নজরে:

- ✅ **Component-based Structure** — ছোট ছোট ইউনিটে কোড ভাগ
- ⚡ **Virtual DOM** — DOM ম্যানিপুলেশন দ্রুত হয়
- 🎯 **Declarative UI** — কী চাই সেটাই বলে দাও, React ব্যবস্থা নেবে
- 🔄 **State & Props** — কম্পোনেন্টে ডেটা ব্যবস্থাপনা
- 🔌 **Hooks** — যেমন `useState`, `useEffect` দিয়ে ফাংশনাল কম্পোনেন্টে ম্যাজিক

---

### 🔹 JavaScript vs React: কখন কোনটা?

| প্রয়োজন                   | Vanilla JS | React            |
| ------------------------- | ---------- | ---------------- |
| ছোট অ্যাপ                 | ✔️         | ❌               |
| বড় অ্যাপ                  | ❌         | ✔️               |
| ইন্টারঅ্যাকটিভ ফিচার      | ❌         | ✔️               |
| Reusable কম্পোনেন্ট       | ❌         | ✔️               |
| পারফরম্যান্স ম্যানেজমেন্ট | ❌         | ✔️ (Virtual DOM) |

---

### শেষ কথা

ভ্যানিলা JS হলো ভিত্তি, আর React হলো সেই ভিত্তির উপর বানানো একটি শক্তিশালী টুল। ছোটখাটো কাজের জন্য ভ্যানিলা JS সেরা, কিন্তু বড় এবং আধুনিক অ্যাপ বানানোর জন্য React তোমার কাজকে অনেক সহজ, দ্রুত এবং আনন্দদায়ক করে তুলবে।

## 📚 শেখার রিসোর্স

- React অফিসিয়াল: [https://react.dev/](https://react.dev)
- বাংলা ভিডিও সিরিজ: [React Bangla Tutorial on YouTube](https://www.youtube.com/watch?v=JSLTS3dfHKU)

---

## ❤️ কমিউনিটির শক্তি

React শেখা মানে শুধু কোড না—কমিউনিটির অংশ হওয়া।
তুমি শুধু শেখো না, শেখাও। নিজের অভিজ্ঞতা অন্যদের সাহায্য করে। একসাথে শেখাই হলো সত্যিকারের শিখন।

---

> আর হ্যাঁ, ভাই — React শেখা একদিনে হবে না, কিন্তু শুরুটা আজই হোক।

---

<hr />
<Donate />
<hr />
<GiscusComments
  repo="codedbyMojnu/react-bangla-tutorial"
  repoId="R_kgDOLDE87Q"
  category="General"
  categoryId="DIC_kwDOLDE87c4CtCIY"
  mapping="pathname"
  inputPosition="top"
  theme="preferred_color_scheme"
  lang="en"
/>
