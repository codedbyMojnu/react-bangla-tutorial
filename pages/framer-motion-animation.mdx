# **Framer Motion: React-এ দারুণ স্মুথ অ্যানিমেশন 🚀**

Framer Motion হলো React এর জন্য সবচেয়ে সহজ, শক্তিশালী এবং প্রোডাকশন-রেডি অ্যানিমেশন লাইব্রেরি।
এটি দিয়ে CSS `@keyframes` এর জটিলতা ছাড়াই খুব সুন্দর অ্যানিমেশন তৈরি করা যায়।

---

## ১. ইনস্টলেশন

প্রথমেই আপনার React প্রজেক্টে Framer Motion যোগ করুন:

```bash
npm install framer-motion
# অথবা
yarn add framer-motion
```

---

## ২. বেসিক অ্যানিমেশন: fade-in + scale-up

```jsx
import { motion } from "framer-motion";

export default function BasicAnimation() {
  return (
    <motion.div
      className="w-40 h-40 bg-red-500 rounded-lg"
      initial={{ opacity: 0, scale: 0.5 }} // শুরু অবস্থা
      animate={{ opacity: 1, scale: 1 }} // শেষ অবস্থা
      transition={{ duration: 0.8 }} // সময়কাল (সেকেন্ড)
    />
  );
}
```

**ব্যাখ্যা:**
`initial` — প্রথম অবস্থা
`animate` — অ্যানিমেশন শেষে অবস্থা
`transition` — কত দ্রুত ও কীভাবে অ্যানিমেশন হবে

---

## ৩. Keyframe অ্যানিমেশন (একাধিক ধাপ)

একাধিক ধাপের অ্যানিমেশন করতে `animate` প্রপ-এ অ্যারে ব্যবহার করুন:

```jsx
import { motion } from "framer-motion";

export default function KeyframeAnimation() {
  return (
    <motion.div
      className="w-40 h-40 bg-blue-500 rounded-lg"
      animate={{
        scale: [1, 1.5, 1, 1.5, 1],
        rotate: [0, 90, 0, -90, 0],
        borderRadius: ["20%", "50%", "20%", "50%", "20%"],
      }}
      transition={{
        duration: 2,
        ease: "easeInOut",
        times: [0, 0.25, 0.5, 0.75, 1],
        repeat: Infinity,
        repeatDelay: 1,
      }}
    />
  );
}
```

---

## ৪. Gesture অ্যানিমেশন: Hover & Tap ইফেক্ট

ইউজারের ইন্টারঅ্যাকশনের জন্য `whileHover` ও `whileTap` ব্যবহার করুন:

```jsx
import { motion } from "framer-motion";

export default function GestureAnimation() {
  return (
    <motion.button
      className="px-6 py-3 bg-gray-800 text-white rounded-lg font-semibold"
      whileHover={{ scale: 1.1, rotate: 3 }}
      whileTap={{ scale: 0.9, rotate: -3 }}
      transition={{ type: "spring", stiffness: 300 }}
    >
      Click Me
    </motion.button>
  );
}
```

---

## ৫. Drag অ্যানিমেশন (সরানোর সুবিধা)

কোনো এলিমেন্টকে মাউস দিয়ে সরানোর সুবিধা দিতে:

```jsx
import { motion } from "framer-motion";

export default function DragExample() {
  return (
    <motion.div
      className="w-32 h-32 bg-green-500 rounded-lg"
      drag
      dragConstraints={{ top: -50, left: -50, right: 50, bottom: 50 }}
    />
  );
}
```

---

## ৬. Variants দিয়ে স্ট্রাকচারড অ্যানিমেশন

একাধিক এলিমেন্টের জন্য সহজে অ্যানিমেশন নিয়ন্ত্রণ করতে Variants ব্যবহার করুন:

```jsx
import { motion } from "framer-motion";

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.3 },
  },
};

const itemVariants = {
  hidden: { y: 20, opacity: 0 },
  visible: { y: 0, opacity: 1 },
};

export default function StaggerList() {
  const items = ["প্রথম", "দ্বিতীয়", "তৃতীয়"];

  return (
    <motion.ul
      className="list-none p-0"
      variants={containerVariants}
      initial="hidden"
      animate="visible"
    >
      {items.map((item, index) => (
        <motion.li
          key={index}
          className="p-4 bg-indigo-500 my-2 rounded text-white"
          variants={itemVariants}
        >
          {item}
        </motion.li>
      ))}
    </motion.ul>
  );
}
```

---

## ৭. Scroll-ভিত্তিক অ্যানিমেশন

স্ক্রল অনুযায়ী অ্যানিমেশন দিতে `useScroll` ও `useTransform` ব্যবহার করা হয়:

```jsx
import { motion, useScroll, useTransform } from "framer-motion";

export default function ScrollAnimation() {
  const { scrollYProgress } = useScroll();
  const scale = useTransform(scrollYProgress, [0, 1], [0.5, 2]);

  return (
    <motion.div
      style={{ scale }}
      className="w-40 h-40 bg-pink-500 rounded-lg"
    />
  );
}
```

---

## ৮. React + Framer Motion দিয়ে Smooth Counter

```jsx
import React, { useEffect } from "react";
import { motion, useMotionValue, useTransform, animate } from "framer-motion";

export default function Counter() {
  const count = useMotionValue(0);
  const rounded = useTransform(count, Math.round);

  useEffect(() => {
    const animation = animate(count, 100, { duration: 2 });
    return animation.stop;
  }, []);

  return (
    <div className="text-5xl font-bold">
      <motion.span>{rounded}</motion.span>
    </div>
  );
}
```

---

## ৯. Scroll Reveal Animation (Fade + Slide)

```jsx
import React from "react";
import { motion } from "framer-motion";

const Box = ({ text }) => (
  <motion.div
    className="w-32 h-32 m-4 rounded bg-pink-300 flex items-center justify-center"
    initial={{ opacity: 0, x: -50 }}
    whileInView={{ opacity: 1, x: 0 }}
    transition={{ duration: 1 }}
    viewport={{ once: true }}
  >
    <span className="text-black text-xl">{text}</span>
  </motion.div>
);

export default function ScrollReveal() {
  const items = ["হ্যালো", "আসসালামু আলাইকুম", "স্বাগতম", "শুভেচ্ছা", "আসুন"];

  return (
    <div className="flex flex-col items-center py-10">
      {items.map((item, i) => (
        <Box key={i} text={item} />
      ))}
    </div>
  );
}
```

---

## ১০. Stagger Animation (সুন্দর পরপর আসা এলিমেন্ট)

```jsx
import { motion } from "framer-motion";

const container = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.3 },
  },
};

const item = {
  hidden: { y: 20, opacity: 0 },
  visible: { y: 0, opacity: 1 },
};

export default function StaggerExample() {
  const list = ["প্রথম", "দ্বিতীয়", "তৃতীয়", "চতুর্থ"];

  return (
    <motion.ul
      variants={container}
      initial="hidden"
      animate="visible"
      className="list-none p-0"
    >
      {list.map((text, idx) => (
        <motion.li
          key={idx}
          variants={item}
          className="bg-blue-500 text-white rounded p-4 my-2"
        >
          {text}
        </motion.li>
      ))}
    </motion.ul>
  );
}
```

---

## সামারি ও টিপস

- React প্রজেক্টে অ্যানিমেশন যোগ করার সবচেয়ে সহজ ও পেশাদারী উপায় হলো Framer Motion।
- Tailwind CSS এর সাথে খুব ভালো কম্বিনেশন।
- Props দিয়ে ডিক্লেয়ারেটিভ অ্যানিমেশন কন্ট্রোল সহজ।
- Variants & Stagger অ্যানিমেশন বড় প্রোজেক্টে কোডকে রিডেবল ও মেইনটেনেবল রাখে।
- Gesture ও Drag ফিচার দিয়ে ইন্টারঅ্যাকশন অনেক স্মুথ হয়।
- স্ক্রল বেসড অ্যানিমেশনও খুব সহজে বানানো যায়।

---
