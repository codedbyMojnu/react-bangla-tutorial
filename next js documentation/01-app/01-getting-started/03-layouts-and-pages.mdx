---
title: Layouts and Pages
description: Learn how to create your first pages and layouts, and link between them with the Link component.
related:
  title: API Reference
  description: Learn more about the features mentioned in this page by reading the API Reference.
  links:
    - app/getting-started/linking-and-navigating
    - app/api-reference/file-conventions/layout
    - app/api-reference/file-conventions/page
    - app/api-reference/components/link
    - app/api-reference/file-conventions/dynamic-routes
---

# Next.js এ Routing, Page আর Layout বোঝা

Next.js এ **file-system based routing** ব্যবহার করা হয়। মানে, `app` ফোল্ডারের ভেতরে ফাইল আর ফোল্ডার তৈরি করলেই স্বয়ংক্রিয়ভাবে route তৈরি হয়ে যায়।

এই টিউটোরিয়ালে আমরা শিখবো:

1. কিভাবে **Page** বানাতে হয়।
2. কিভাবে **Layout** বানাতে হয়।
3. কিভাবে এগুলো ব্যবহার করে Blog Application বানানো যায়।

---

## ১. Page তৈরি করা

একটা **Page** মানে হচ্ছে, কোনো নির্দিষ্ট route (URL) এ গেলে যে UI দেখা যাবে।

👉 ধরো, আমরা আমাদের ব্লগ অ্যাপের **হোমপেজ** (`/`) বানাতে চাই। এজন্য আমাদের `app` ফোল্ডারের ভেতরে `page.js` নামে ফাইল বানাতে হবে।

```jsx filename="app/page.js"
export default function Page() {
  return <h1>আমার ব্লগে স্বাগতম!</h1>;
}
```

🔗 এবার ব্রাউজারে `/` route এ গেলে তুমি এই লেখা দেখতে পাবে:
**"আমার ব্লগে স্বাগতম!"**

➡️ একইভাবে, যদি তুমি `/about` নামে একটা route বানাতে চাও, তাহলে `app/about/page.js` ফাইল তৈরি করলেই হবে:

```jsx filename="app/about/page.js"
export default function AboutPage() {
  return <h1>এই ব্লগ সম্পর্কে</h1>;
}
```

---

## ২. Layout তৈরি করা

**Layout** হচ্ছে সেই UI যেটা অনেকগুলো পেজে **একই রকম থাকবে**। যেমন আমাদের ব্লগ অ্যাপে **Header, Footer, Sidebar** সব পেজেই একরকম থাকবে।

Next.js এ Layout বানাতে হলে `layout.js` নামে ফাইল তৈরি করতে হয়। এই ফাইলের ভেতরে একটি React component লিখতে হবে, যেখানে `children` নামের prop থাকবে।

👉 ধরো, আমরা ব্লগ অ্যাপের Root Layout বানাচ্ছি:

```jsx filename="app/layout.js"
export default function BlogLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <header>
          <h2>My Blog</h2>
          <nav>
            <a href="/">হোম</a> |<a href="/about">আমাদের সম্পর্কে</a> |
            <a href="/posts">সব পোস্ট</a>
          </nav>
        </header>

        <main>{children}</main>

        <footer>
          <p>© 2025 My Blog. সর্বস্বত্ব সংরক্ষিত।</p>
        </footer>
      </body>
    </html>
  );
}
```

### কী হলো এখানে?

- **Header** সব পেজে থাকবে।
- **Footer** সব পেজে থাকবে।
- `children` যেখানে থাকবে, সেখানেই নির্দিষ্ট পেজের কনটেন্ট বসবে।

---

## ৩. Blog Application Example

এখন ধরো, আমরা `/posts` নামে একটা পেজ বানাচ্ছি যেখানে ব্লগ পোস্টের লিস্ট থাকবে।

```jsx filename="app/posts/page.js"
export default function PostsPage() {
  return (
    <div>
      <h1>সব ব্লগ পোস্ট</h1>
      <ul>
        <li>👉 Next.js কীভাবে কাজ করে?</li>
        <li>👉 React Hook সহজভাবে বোঝা</li>
        <li>👉 Tailwind CSS দিয়ে ডিজাইন</li>
      </ul>
    </div>
  );
}
```

এবার `/posts` route এ গেলে তুমি সব পোস্টের লিস্ট দেখতে পাবে।

---

## সংক্ষেপে

- **page.js** = নির্দিষ্ট পেজ বানাতে।
- **layout.js** = সব পেজের জন্য কমন ডিজাইন রাখতে।
- **children** = যেখানে প্রতিটা পেজের কনটেন্ট বসবে।

এভাবে তুমি Next.js ব্যবহার করে একটা সম্পূর্ণ ব্লগ অ্যাপ বানাতে পারবে।

---

খুব ভালো 👍 এবার আমি তোমাকে গল্পের মতো করে সহজ বাংলায় বুঝাই কিভাবে **Nested Route** কাজ করে Next.js এ। আমরা আবারো **Blog Application** উদাহরণ ব্যবহার করবো, যাতে বিষয়টা পরিষ্কার হয়।

---

# Nested Route বোঝা (গল্প দিয়ে)

ভাবো, তুমি একটা **ব্লগ ওয়েবসাইট** বানাচ্ছো।
প্রথমে তুমি শুধু হোমপেজ বানালে → `/`
তারপর তুমি একটা **Blog Section** বানালে → `/blog`
এখন তুমি চাইছো, প্রতিটি ব্লগ পোস্টের জন্য আলাদা আলাদা পেজ হোক। যেমন:

- `/blog/first-post`
- `/blog/nextjs-routing`
- `/blog/my-life-story`

এটাই হলো **Nested Route**।
মানে হচ্ছে → **URL এ একটার ভেতরে আরেকটা সেগমেন্ট (part) বসানো।**

---

## Nested Route কীভাবে কাজ করে?

Next.js এ **Folder** আর **File** দিয়ে Nested Route বানানো হয়।

- **Folder** মানে একটা Route Segment।
- **File (page.js/layout.js)** মানে ওই Segment এ কোন UI দেখানো হবে।

---

## ধাপ ১: `/blog` রুট তৈরি করা

প্রথমে আমরা `app` ফোল্ডারের ভেতরে `blog` নামে একটা ফোল্ডার বানাই।
এরপর এর ভেতরে `page.js` বানাই।

```jsx filename="app/blog/page.js"
export default function BlogPage() {
  return (
    <div>
      <h1>সব ব্লগ পোস্ট</h1>
      <ul>
        <li>
          <a href="/blog/first-post">👉 প্রথম ব্লগ পোস্ট</a>
        </li>
        <li>
          <a href="/blog/nextjs-routing">👉 Next.js Routing শেখা</a>
        </li>
        <li>
          <a href="/blog/my-life-story">👉 আমার জীবনের গল্প</a>
        </li>
      </ul>
    </div>
  );
}
```

➡️ এখন `/blog` রুট এ গেলে সব ব্লগ পোস্টের লিস্ট দেখা যাবে।

---

## ধাপ ২: Dynamic Nested Route `[slug]`

এখন তুমি চাইছো, প্রতিটি ব্লগ পোস্ট আলাদা আলাদা পেজে খোলা যাবে।
এর জন্য `blog` ফোল্ডারের ভেতরে `[slug]` নামে আরেকটা ফোল্ডার বানাতে হবে।

👉 এখানে `[slug]` মানে **Dynamic Route Segment**।
মানে, এই অংশটা প্রতিবার আলাদা হবে। যেমন:

- `/blog/first-post`
- `/blog/nextjs-routing`
- `/blog/my-life-story`

### উদাহরণ:

```jsx filename="app/blog/[slug]/page.js"
export default function BlogPostPage({ params }) {
  return (
    <div>
      <h1>এটি একটি ব্লগ পোস্ট</h1>
      <p>তুমি এখন পড়ছো: {params.slug}</p>
    </div>
  );
}
```

➡️ এখন যদি তুমি ব্রাউজারে যাও `/blog/first-post`, তবে দেখাবে:
**"তুমি এখন পড়ছো: first-post"**

---

## ধাপ ৩: গল্প দিয়ে বোঝা

ভাবো তোমার বাড়ি একটা গ্রাম।

- **Root `/`** = তোমার বাড়ির গেট।
- **`/blog`** = তোমার উঠোন, যেখানে অনেকগুলো গল্প রাখা আছে।
- **`/blog/[slug]`** = প্রতিটি আলাদা কক্ষ (ঘর), যেখানে নির্দিষ্ট একটা গল্প আছে।

প্রথমে তুমি উঠোনে ( `/blog` ) গেলে সব গল্পের নাম দেখা যাবে।
তারপর যেকোনো নাম চাপ দিলে, তুমি নির্দিষ্ট ঘরে ঢুকবে আর সেই গল্প পড়বে।

এটাই হলো **Nested Route** আর **Dynamic Route** একসাথে ব্যবহার।

---

## সংক্ষেপে

1. **Folder** = Route বানায়।
2. **page.js** = UI দেখায়।
3. **Nested Folder** = Nested Route বানায়।
4. **\[slug] Folder** = Dynamic Route, যেখানে প্রতিটি ব্লগ পোস্ট আলাদা আলাদা URL পায়।

---

দারুণ প্রশ্ন 👍
এবার আমরা দেখবো কিভাবে **ডাটাবেস বা API** থেকে ব্লগ পোস্টের লিস্ট নিয়ে আসবো এবং **Dynamic Route** ব্যবহার করে প্রতিটি পোস্ট আলাদা পেজে দেখাবো।
আমি গল্পের মতো করে বলছি, তারপর কোড দেবো।

---

এবং প্রতিটি পোস্টে ক্লিক করলে `/blog/[slug]` এ গিয়ে সেই পোস্ট দেখা যাবে।

---

## ধাপ ২: ডাটাবেস বা API থেকে ডাটা আনা

এখানে ডাটাবেস ধরতে পারি MySQL, MongoDB বা অন্য কিছু।
কিন্তু সহজ বোঝানোর জন্য আমরা আপাতত একটা **Fake API** ব্যবহার করবো।

আমরা ধরে নিচ্ছি, API থেকে আমরা এইরকম ডাটা পাচ্ছি:

```json
[
  {
    "id": 1,
    "title": "Next.js Routing শেখা",
    "slug": "nextjs-routing",
    "content": "এখানে আমরা Next.js routing সম্পর্কে শিখবো..."
  },
  {
    "id": 2,
    "title": "React Hook সহজভাবে",
    "slug": "react-hooks",
    "content": "React Hook খুব সহজভাবে ব্যবহার করা যায়..."
  }
]
```

---

## ধাপ ৩: `/blog` পেজে সব পোস্ট দেখানো

এবার আমরা API থেকে ডাটা নিয়ে `/blog/page.js` এ পোস্টের লিস্ট দেখাবো।

```jsx filename="app/blog/page.js"
async function getPosts() {
  // এখানে আমরা Fake API ব্যবহার করছি
  const res = await fetch("https://example.com/api/posts");
  return res.json();
}

export default async function BlogPage() {
  const posts = await getPosts();

  return (
    <div>
      <h1>সব ব্লগ পোস্ট</h1>
      <ul>
        {posts.map((post) => (
          <li key={post.id}>
            <a href={`/blog/${post.slug}`}>{post.title}</a>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

➡️ এখন `/blog` রুটে গেলে তুমি সব পোস্টের নাম দেখবে।
প্রতিটি পোস্টে ক্লিক করলে `/blog/[slug]` এ যাবে।

---

## ধাপ ৪: Dynamic Route দিয়ে একক পোস্ট দেখানো

এবার `[slug]` ফোল্ডারের ভেতরে `page.js` বানাতে হবে।
এখানে আমরা URL থেকে `slug` নিয়ে নির্দিষ্ট পোস্ট বের করবো।

```jsx filename="app/blog/[slug]/page.js"
async function getPost(slug) {
  const res = await fetch(`https://example.com/api/posts/${slug}`);
  return res.json();
}

export default async function BlogPostPage({ params }) {
  const post = await getPost(params.slug);

  return (
    <article>
      <h1>{post.title}</h1>
      <p>{post.content}</p>
    </article>
  );
}
```

➡️ এখন যদি তুমি যাও `/blog/nextjs-routing`,
তাহলে দেখাবে **"Next.js Routing শেখা"** পোস্টের বিস্তারিত।

---

## ধাপ ৫: গল্প দিয়ে বোঝা

ভাবো তোমার কাছে একটা **গল্পের লাইব্রেরি (API)** আছে।

- `/blog` মানে হলো লাইব্রেরির গেট, যেখানে সব বইয়ের নাম লিস্ট আকারে আছে।
- প্রতিটি বইয়ের নামের সাথে একটা লিঙ্ক আছে (slug)।
- যখন তুমি কোনো বইয়ে ক্লিক করো, তুমি আসলে `/blog/[slug]` ঘরে ঢুকছো।
- আর ভেতরে ঢুকলে তুমি বইয়ের ভেতরের গল্প (content) পড়তে পারছো।

---

## সংক্ষেপে

1. **API থেকে ডাটা আনা** – `fetch()` দিয়ে।
2. **/blog পেজে লিস্ট দেখানো** – সব পোস্টের নাম দেখাও, লিঙ্ক করে দাও।
3. **/blog/\[slug] পেজে নির্দিষ্ট পোস্ট দেখানো** – URL এর slug ব্যবহার করে ডাটা বের করো।

---

## **Nested Layouts + Dynamic Segments **

---

## **১. Nested Layout কী?**

ধরো, তুমি একটা ব্লগ অ্যাপ বানাচ্ছো।

- সব পেজে হেডার ও ফুটার থাকবে।
- `/blog` route-এর জন্য আলাদা section লাগবে।

Nested Layout মানে: **বড় লেআউটের ভিতরে ছোট লেআউট থাকে**।

**Flow:**

```
Root Layout → Blog Layout → Blog Page
```

---

### **JS কোড উদাহরণ**

**1️⃣ Root Layout (`app/layout.js`)**

```js
export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <header>আমার ব্লগের হেডার</header>
        {children} {/* সব child layout এবং পেজ এখানে দেখাবে */}
        <footer>ফুটার</footer>
      </body>
    </html>
  );
}
```

**2️⃣ Blog Layout (`app/blog/layout.js`)**

```js
export default function BlogLayout({ children }) {
  return (
    <section>
      <h2>Blog Section</h2>
      {children} {/* ব্লগ পেজ বা ব্লগ পোস্ট এখানে দেখাবে */}
    </section>
  );
}
```

এখন `/blog` route-এর পেজ এবং প্রতিটি পোস্ট **Blog Layout এর ভিতরে**, আর Blog Layout আবার **Root Layout** এর ভিতরে থাকবে।

---

## **২. Dynamic Segment কী?**

ধরো, তোমার ব্লগে অনেক পোস্ট আছে।
প্রতিটি পোস্টের জন্য আলাদা route বানানো কঠিন।

এক্ষেত্রে **dynamic segment** ব্যবহার করা হয়।

- Dynamic Segment হলো **ডেটার উপর ভিত্তি করে route বানানো**।
- Folder name এর মধ্যে **square bracket** `[slug]` দিয়ে dynamic route তৈরি করা হয়।

---

### **JS কোড উদাহরণ**

**Folder Structure:**

```
app/
 └─ blog/
     ├─ page.js          // ব্লগ লিস্ট দেখাবে
     └─ [slug]/
         └─ page.js      // ব্লগ পোস্ট দেখাবে
```

**Blog Post Page (`app/blog/[slug]/page.js`)**

```js
export default async function BlogPostPage({ params }) {
  const { slug } = params;

  // ধরো getPost() ফাংশন ডেটা নিয়ে আসে
  const post = await getPost(slug);

  return (
    <div>
      <h1>{post.title}</h1>
      <p>{post.content}</p>
    </div>
  );
}
```

যখন ইউজার `/blog/my-first-post` ভিজিট করবে,
`slug = "my-first-post"` হবে।

---

## **৩. Nested Layout + Dynamic Segment**

আমরা Nested Layout এবং Dynamic Segment একসাথে ব্যবহার করতে পারি।

```
app/
 └─ layout.js          // Root Layout
 └─ blog/
     └─ layout.js      // Blog Layout
     └─ [slug]/
         └─ page.js    // Dynamic Blog Post
```

Flow:

1. Root Layout → Header/Footer
2. Blog Layout → Blog Section
3. Blog Post Page → Post Content

---

## **৪. গল্পের মতো বোঝা**

- Root Layout = মাতার কোল → সবকিছু ধরে রাখে
- Blog Layout = ব্লগের রুম → সব ব্লগ এখানে থাকে
- `[slug]` = প্রতিটি পোস্টের বেডরুম → শুধুই ওই পোস্টের কনটেন্ট

Nested Layout + Dynamic Segment মিলিয়ে তুমি **বড় অ্যাপ clean এবং readable রাখো**।

---

## **১. Rendering with Search Params (সার্চ প্যারাম দিয়ে পেজ দেখানো)**

ধরো, তুমি একটি ব্লগ লিস্ট বানাচ্ছো। ইউজার চাইলে URL-এ `?category=tech` লিখে শুধু টেকনোলজি পোস্ট দেখতে পারে।

**Server Component পেজে** search parameters পেতে আমরা `searchParams` prop ব্যবহার করি।

### **JS কোড উদাহরণ (Server Component)**

```jsx
// app/page.jsx
export default async function Page({ searchParams }) {
  const category = (await searchParams).category; // URL থেকে category নাও

  const posts = await getPosts(); // সব পোস্ট নাও
  const filteredPosts = category
    ? posts.filter((post) => post.category === category)
    : posts;

  return (
    <div>
      <h1>Blog List</h1>
      <ul>
        {filteredPosts.map((post) => (
          <li key={post.slug}>{post.title}</li>
        ))}
      </ul>
    </div>
  );
}
```

**ক্লায়েন্ট সাইডে** search params পেতে `useSearchParams` হুক ব্যবহার করতে পারো।

- উদাহরণ: Already loaded list filter করা।

**মনে রাখার বিষয়:**

- `searchParams` → **Server Component**, ডেটা লোডের জন্য।
- `useSearchParams` → **Client Component**, UI ফিল্টার বা ছোট কাজের জন্য।

---

## **২. Linking Between Pages (পেজের মধ্যে লিঙ্ক করা)**

ধরো তুমি ব্লগ লিস্ট বানিয়েছো। প্রতিটি পোস্ট ক্লিক করলে আলাদা পেজে যাবে।

Next.js-এ `<Link>` ব্যবহার করলে **client-side navigation এবং prefetching** পাওয়া যায়।

### **JS কোড উদাহরণ**

```jsx
// app/ui/post.jsx
import Link from "next/link";

export default async function PostList() {
  const posts = await getPosts();

  return (
    <ul>
      {posts.map((post) => (
        <li key={post.slug}>
          <Link href={`/blog/${post.slug}`}>{post.title}</Link>
        </li>
      ))}
    </ul>
  );
}
```

**কাজের ধরন:**

1. ইউজার `/blog/my-first-post` ক্লিক করলে নতুন পেজ লোড হবে।
2. `<Link>` ব্যবহার করলে পেজ দ্রুত লোড হবে।
3. চাইলে `useRouter` দিয়ে আরও advanced navigation করা যায়।

---

## **Route Props Helpers (পেজ ও লেআউটের props সহজভাবে নেওয়া)**

Next.js আমাদের জন্য কিছু helper দেয় যা route থেকে props নিতে সহজ করে:

- **PageProps** → পেজের `params` এবং `searchParams` পেতে।
- **LayoutProps** → লেআউটের `children` এবং named slots (যেমন `@analytics`) পেতে।

### **JSX উদাহরণ: PageProps**

```jsx
// app/blog/[slug]/page.jsx
export default async function Page(props) {
  const { slug } = await props.params; // URL থেকে slug নাও
  return <h1>Blog post: {slug}</h1>;
}
```

**ব্যাখ্যা:**

- ধরো URL হচ্ছে `/blog/my-first-post`, তাহলে `slug` হবে `"my-first-post"`.
- পেজটি স্বয়ংক্রিয়ভাবে route অনুযায়ী params নেবে।

---

### **JSX উদাহরণ: LayoutProps**

```jsx
// app/dashboard/layout.jsx
export default function Layout(props) {
  return (
    <section>
      {props.children} {/* এখানে সব child পেজ বা কম্পোনেন্ট দেখাবে */}
      {/* যদি app/dashboard/@analytics থাকে, এটি named slot হিসাবে পাওয়া যাবে */}
      {/* {props.analytics} */}
    </section>
  );
}
```

**ব্যাখ্যা:**

- `children` → যেকোনো পেজ বা কম্পোনেন্ট যা লেআউটের ভিতরে রেন্ডার হবে।
- Named slots → optional, যেমন `@analytics` ফোল্ডার থাকলে props হিসেবে পাওয়া যায়।

---

**মনে রাখার বিষয়:**

- Static routes → `params` সাধারণত `{}`।
- PageProps ও LayoutProps → global helper, কোনো import দরকার নেই।
- Types generate হয় `next dev`, `next build` বা `next typegen` চালালে।

---
